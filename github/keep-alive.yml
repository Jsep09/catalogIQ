name: Keep Supabase Alive
on:
  schedule:
    - cron: '0 0 * * *'  # ทุกวันตี 0
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Supabase
        run: curl https://xxxx.supabase.co/rest/v1/ -H "apikey: ${{ secrets.SUPABASE_KEY }}"
